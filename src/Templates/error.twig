<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error {{ code }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="d-flex align-items-center justify-content-center vh-100">
    <div class="text-center">
        <h1 class="display-1 fw-bold">{{ code }}</h1>
        <p class="fs-3"> <span class="text-danger">Opps!</span> {{ message }}.</p>
        <a href="{{ default_page }}" class="btn btn-primary">Go Home</a>
        {% if debug %}
            <div style="height: 300px; width: 90%; overflow: auto; border: 1px solid black; margin-top: 15px; text-align: left; padding: 5px;">
                <pre style="max-width: 1000px">{{ throwable | raw }}</pre>
            </div>
        {% endif %}
    </div>
</div>
</body>
</html>